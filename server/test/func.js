/**
 * getIndex method returns the indexed words and the documents that were found
 * @param {int} n - The number to check if even
 * @return {boolean} - returns a boolean
*/
export default function isEven(n) {
  return n % 2 === 0;
}
